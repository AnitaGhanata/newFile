//Q.4 Write a C program to compare two strings.
let str1 = "Apple", str2 = "Apple";
    if(str1==str2){
        result= "string are same";
    }
    else{
        result = "string are not same"
    }
    console.log(result);

    //Output = string are same

